#include <stdio.h>
int main() {
	int n, x, a, b, sum = 0;
	scanf("%d", &x);
	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
		scanf_s("%d %d", &a, &b);
		sum += a * b;
	}
	if (x == sum) {
		printf("Yes");
	}
	else {
		printf("No");
	}
	return 0;
}
